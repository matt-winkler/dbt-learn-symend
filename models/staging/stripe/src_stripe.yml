version: 2

sources:
  - name: stripe
    description: copy of stripe database
    tables:
      - name: payment
        description: raw payment data
        columns:
          - name: id
            description: primary key for payments
            tests:
              - unique
              - not_null